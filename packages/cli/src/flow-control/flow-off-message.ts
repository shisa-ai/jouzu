/** One refusal text for every flow tool, so "off" reads the same wherever it is met. */
export const FLOW_OFF_MESSAGE =
	"Flow control is off for this session; run /flow on to use flow tools. Producer tools and their own delivery paths still work.";
