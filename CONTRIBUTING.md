# Contributing

Jouzu is pre-release. Open an issue before starting a substantial change.

Run the local release gate before submitting a pull request:

```bash
npm ci --ignore-scripts
npm run release:check
```
